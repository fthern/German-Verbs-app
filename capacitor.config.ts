import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.youware.germanverbs',
  appName: 'German Verbs',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
